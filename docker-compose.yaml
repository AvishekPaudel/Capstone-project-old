version: '3.9'

services:
  web:
    build: .
    container_name: pasal-app
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - sqlite_data:/app/src

volumes:
  sqlite_data: